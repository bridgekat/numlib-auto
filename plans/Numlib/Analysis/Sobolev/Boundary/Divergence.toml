# The divergence theorem on the region above the graph of a `C¹` function (decision B4 of
# `notes/boundary/planning-brief.md`) and its transport to a graph chart of a domain. The module
# exists with a doc comment and every node is proved and documented, so only the ids remain.

[[node]]
id = "EuclideanSpace.integral_div_eq_zero_of_hasCompactSupport"

[[node]]
id = "EuclideanSpace.integral_epigraph_eq"

[[node]]
id = "EuclideanSpace.lintegral_epigraph_eq"

[[node]]
id = "EuclideanSpace.integrableOn_fderiv_apply_epigraph"

[[node]]
id = "EuclideanSpace.hasCompactSupport_comp_snocLast_graph"

[[node]]
id = "EuclideanSpace.integral_Ioi_fderiv_last_eq"

[[node]]
id = "EuclideanSpace.integral_fderiv_last_epigraph"

[[node]]
id = "EuclideanSpace.integral_fderiv_castSucc_epigraph"

[[node]]
id = "EuclideanSpace.inner_snocLast_neg_one"

[[node]]
id = "EuclideanSpace.integral_div_epigraph"

[[node]]
id = "EuclideanSpace.integral_div_graphEpigraph"

[[node]]
id = "IsBoundaryGraphAt.integral_div_eq_of_tsupport_subset"
