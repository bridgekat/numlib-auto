namespace = "Matrix"

[[node]]
id = "sylvesterMap"

[[node]]
id = "sylvesterMap_apply"

[[node]]
id = "aeval_mul_eq_mul_aeval_of_mul_eq_mul"

[[node]]
id = "sylvesterMap_injective_of_isCoprime"

[[node]]
id = "sylvesterMap_bijective_of_isCoprime"

[[node]]
id = "sylvesterMap_vecMulVec_eq_zero"

[[node]]
id = "sylvesterMap_bijective_iff_disjoint_spectrum"

[[node]]
id = "isCoprime_charpoly_iff_disjoint_spectrum_complexify"

[[node]]
id = "existsUnique_sylvesterMap_eq"

[[node]]
id = "fromBlocks_one_neg_mul_fromBlocks_mul_fromBlocks_one"

[[node]]
id = "exists_fromBlocks_conj_eq_fromBlocks_zero"

[[node]]
id = "exists_isUnit_conj_eq_blockDiagonalPart"

[[node]]
id = "sep"

[[node]]
id = "sep_nonneg"

[[node]]
id = "exists_sep_eq"

[[node]]
id = "sep_pos_iff"

[[node]]
id = "frobenius_norm_le_div_sep"

[[node]]
id = "sep_le_norm_sub"

[[node]]
id = "sep_unitary_conj"

[[node]]
id = "sep_comm"

[[node]]
id = "sep_sub_le_sep_add"

[[node]]
id = "sep_eq_iInf_singularValues"
kind = "theorem"
desc = '''The `1 × 1` case ([golub2013matrix] proof of Corollary 7.2.6): for `c : 𝕜` and `T : Matrix n n 𝕜`,
`sep (of fun _ _ => c : Matrix Unit Unit 𝕜) T = ⨅ i, (T - c • 1).singularValues i` — the smallest
singular value of `T - c I`. A `1 × n` unknown is a row vector `xᵀ`, `‖c xᵀ - xᵀ T‖ = ‖(c • 1 - T)ᵀ x‖`,
and the least value of `‖Mᵀ x‖` on the unit sphere is that of `‖M x‖` (`sep_comm`'s adjoint
argument, or the SVD bridge `Matrix.iInf_singularValues_eq_iInf_norm` staged by ch07 for `SVD`).
(Column-indexed `Matrix.singularValues` on purpose: `T - c • 1` is square, so the `⨅` over its `n`
columns is `σ_min` with no sorted-index bookkeeping; in the sorted reading it is
`(T - c • 1).sortedSingularValues (Fintype.card n - 1)`.) ★

Blocked: on `Matrix.iInf_singularValues_eq_iInf_norm` (open, in `Numlib/LinearAlgebra/Matrix/SVD`, a
group no agent of wave 1 owns). Everything else is in place: `sep_comm` turns the `1 × n` unknown
into an `n × 1` column `x` with `‖T x - x c‖_F = ‖(T - c • 1) x‖₂`, and `le_sep`, `sep_le_norm`,
`exists_sep_eq` match the two infima; about 30 lines once the bridge exists.'''
deps = ["exists_sep_eq", "sep_comm", "Matrix.iInf_singularValues_eq_iInf_norm"]

[[node]]
id = "IsHermitian.sep_eq_iInf_abs_eigenvalues_sub"

[[node]]
id = "sylvesterMap_diagonal_apply"

[[node]]
id = "sylvesterMap_diagonal_eq_iff"

[[node]]
id = "apply_eq_of_sylvesterMap_diagonal_eq"

[[node]]
id = "sylvesterMap_conj"

[[node]]  # added by syl1: unique solvability over any field (split off `existsUnique_sylvesterMap_eq`)
id = "existsUnique_sylvesterMap_eq_of_isCoprime"

[[node]]  # added by syl1: the reindexing form of `sylvesterMap_conj`'s desc
id = "sylvesterMap_submatrix"

[[node]]  # added by syl1: the eigenvector computation behind the kernel element and `sep_le_norm_sub`
id = "sylvesterMap_vecMulVec"

[[node]]  # added by syl1: the inverse clause of the block computation
id = "fromBlocks_one_neg_mul_fromBlocks_one"

[[node]]  # added by syl1: coprime characteristic polynomials iff disjoint spectra, algebraically closed field
id = "isCoprime_charpoly_iff_disjoint_spectrum"

[[node]]  # added by syl1: a left eigenvector for every eigenvalue
id = "exists_vecMul_eq_smul_of_mem_spectrum"

[[node]]  # added by syl1: Theorem 7.1.6 over any field
id = "exists_isUnit_conj_eq_blockDiagonalPart_of_isCoprime"

[[node]]  # added by syl1: the `min_{X ≠ 0}` form of (7.2.5), split off `exists_sep_eq`
id = "sep_mul_norm_le"

[[node]]  # added by syl1
id = "sep_le_norm"

[[node]]  # added by syl1
id = "le_sep"

[[node]]  # added by syl1
id = "sep_of_isEmpty"

[[node]]  # added by syl1
id = "sep_le_norm_of_sylvesterMap_eq_smul"

[[node]]  # added by syl1: the disjoint-spectrum clause, split off `sep_pos_iff`
id = "sep_pos_iff_disjoint_spectrum"

[[node]]  # added by syl1: the Frobenius pairing behind `sep_comm`
id = "re_trace_conjTranspose_mul_le"

[[node]]  # added by syl1
id = "trace_conjTranspose_mul_sylvesterMap"

[[node]]  # added by syl1
id = "le_norm_sylvesterMap_conjTranspose_of_le"

[[node]]  # added by syl1
id = "sep_conjTranspose"

[[node]]  # added by syl1: the diagonal case behind the Hermitian formula
id = "sep_diagonal"
